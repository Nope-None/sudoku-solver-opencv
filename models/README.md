The trained model file `digit_classifier.h5` is generated here when you run:

    python main.py --train

It is excluded from version control via `.gitignore` because of file size.
After training once, it will be reused automatically on every subsequent run.
